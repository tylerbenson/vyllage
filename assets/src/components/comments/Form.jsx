var React = require('react');

var CommentForm = React.createClass({
  render: function () {
    return (
      <div>
        <textarea></textarea>
      </div>
    );
  }
});

module.exports = CommentForm;